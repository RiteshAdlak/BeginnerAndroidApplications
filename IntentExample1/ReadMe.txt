This Application has Button " START B" and " Result " field.

On clicking "START B" it takes user to next Page(Intent) where it has following fields :
1. Enter No. 1
2. Enter No. 2
3. SUBMIT

On giving inputs to numeric fields and submitting  , it adds the numbers and gives the result back to "Result " field on First Page. 